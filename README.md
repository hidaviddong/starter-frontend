# My-FrontEnd-StartUp

## Stack

+ React + TypeScript
+ React Router
+ Vite
+ Zustand + SWR
+ TailwindCSS + NextUI
+ Docker

## How to use
+ Replace `my_app` in Docker
+ Replace project name in package.json
+ `pnpm install` and `pnpm run dev`